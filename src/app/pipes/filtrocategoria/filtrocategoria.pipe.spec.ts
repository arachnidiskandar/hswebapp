import { FiltrocategoriaPipe } from './filtrocategoria.pipe';

describe('FiltrocategoriaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrocategoriaPipe();
    expect(pipe).toBeTruthy();
  });
});
